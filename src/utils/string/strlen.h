#ifndef __EOS_UTILS_STRING_STRLEN_H__
    #define __EOS_UTILS_STRING_STRLEN_H__

    #include "types.h"

/*
* @brief: a function to get the lenght of a string
* @param str: the string that will recover the size
* @return: the value of the lenght of the string
*/
size_t strlen(const char *str);

#endif
