#ifndef __EOS_UTILS_STRING_H__
    #define __EOS_UTILS_STRING_H__

/**
 * @brief      This is the global API for the string utils.
 * all the functions that could be called by other files must be included here.
 * And this file must be the only one to be included when using the string utils.
*/

    #include "string/itoa.h"
    #include "string/revstr.h"

#endif